[defaults]
inventory = inventory
host_key_checking = False
remote_user = ubuntu
roles_path = ./roles
retry_files_enabled = False

[privilege_escalation]
become = True
become_method = sudo
become_user = root
